using DiceRoller.Views;

namespace DiceRoller.Common
{
    internal class Constants
    {
        public class Views
        {
            public const string CREATE_DICE = nameof(CreateDiceView);
            public const string DICE_MAIN_MENU = nameof(DiceMainMenuView);
            public const string LIST_DICE = nameof(ListDiceView);
            public const string SETTINGS = nameof(SettingsView);
        }
    }
}
